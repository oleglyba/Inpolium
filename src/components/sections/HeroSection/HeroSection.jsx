import React from 'react';
import './HeroSection.scss';

const HeroSection = () => {
    return (
        <section className="hero-section">
            <h1>
                Unlocking the <span className="highlight">Data of Tomorrow</span>, today
            </h1>
        </section>
    );
};

export default HeroSection;
